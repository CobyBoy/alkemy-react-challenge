const appBar = { position: 'relative' };

const navLink = {
  textDecoration: 'none',
  color: 'inherit',
  alignSelf: 'center',
  margin: '0.5rem',
};

const toolBar = { justifyContent: 'flex-end' };

const button = {
  color: 'inherit',
  border: '1px solid #ffffff',
  variant: () => 'outlined',
  margin: '0px 0px 1rem 0px'
};

const styles = {
  navLink,
  appBar,
  toolBar,
  button,
};

export default styles;