const typo = {
  textAlign: 'center',
  variant: () => 'h5',
  padding: '2rem',
  
};

const paper = () => {
  return 20;
};

const box = {
  margin: '1rem'
};

const styles = {
  typo,
  paper,
  box
};

export default styles;