const Card = {
  maxWidth: '400',
  margin: '2rem',
  minWidth: '25rem',
  minHeight: '33rem',
};

const CardContent = { cursor: 'pointer' };

const CardMedia = {
  width:'50%',
  height:'50%'
};

const Styles = {
  Card,
  CardContent,
  CardMedia,
};

export default Styles;