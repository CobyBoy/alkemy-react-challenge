const Card = {
  maxWidth: 400,
  margin: '2rem',
  minWidth: '25rem',
  minHeight: '33rem',
  position: 'relative'
};

const CardContent = { cursor: 'pointer' };

const CardActions = {
  display: 'flex',
  justifyContent: 'center',
  margin:'1rem'
};

const Button = {
  width: '96%',
  position: 'absolute',
  bottom: '0.5rem',
};

const Styles = {
  Card,
  CardContent,
  CardActions,
  Button,
};

export default Styles;