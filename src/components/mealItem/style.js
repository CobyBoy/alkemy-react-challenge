const Card = {
  maxWidth: 400,
  margin: '2rem',
  minWidth: '25rem',
  minHeight: '33rem',
  position: 'relative'
};

const CardContent = { cursor: 'pointer' };

const CardActions = {
  display: 'flex',
  justifyContent: 'center',
  margin:'1rem'
};

const Styles = {
  Card,
  CardContent,
  CardActions,
};

export default Styles;