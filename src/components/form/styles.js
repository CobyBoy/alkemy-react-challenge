const InputField = {
  border: '1px solid rgba(0, 0, 0, 0.42)',
  boxSizing: 'border-box',
  width: '100%',
  padding: '16.5px 14px',
  borderRadius: '0.2rem',
  fontSize: 'inherit',
};

const Label = {
  display: 'block',
  marginBottom:'0.6rem'
};

const Box = {
  margin: '0.5rem',
  padding:'1rem'
};

const ErrorMessage = {
  color: 'red',
};

const styles = {
  InputField,
  Box,
  ErrorMessage,
  Label,
};

export default styles;